<ContainerButton xmlns="https://spacestation14.io"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:uik="clr-namespace:Content.Client.UIKit.Controls">
    <BoxContainer Orientation="Horizontal" HorizontalExpand="True" SeparationOverride="0" Name="InternalHBox">
        <SpriteView Scale="2 2" OverrideDirection="South" Name="View" />

        <Label Name="DescriptionLabel" ClipText="True" HorizontalExpand="True" />

        <uik:UIKButton Name="DeleteButton"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Text="{Loc 'character-setup-gui-character-picker-button-delete-button'}" />

        <uik:UIKButton Name="ConfirmDeleteButton"
                              Color="Danger"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Text="{Loc 'character-setup-gui-character-picker-button-confirm-delete-button'}"
                              Visible="False" />
    </BoxContainer>
</ContainerButton>
